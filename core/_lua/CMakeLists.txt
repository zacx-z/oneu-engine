AUX_SOURCE_DIRECTORY(. LUA_SRC_LIST)
AUX_HEADER_DIRECTORY(. LUA_HEAD_LIST)
ADD_LIBRARY(${LUA_LIB} SHARED ${LUA_SRC_LIST} ${LUA_HEAD_LIST})
SET_TARGET_PROPERTIES(${LUA_LIB} PROPERTIES
	PREFIX "" IMPORT_PREFIX ""
	COMPILE_DEFINITIONS LUA_BUILD_AS_DLL
	RUNTIME_OUTPUT_DIRECTORY ${BIN_DIR}
	)

