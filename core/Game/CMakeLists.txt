AUX_SOURCE_DIRECTORY(. GAME_SRC_LIST)
AUX_HEADER_DIRECTORY(. GAME_HEAD_LIST)
ADD_EXECUTABLE(Game WIN32 ${GAME_SRC_LIST} ${GAME_HEAD_LIST})
TARGET_LINK_LIBRARIES(Game ${OUE_LIB})
SET_TARGET_PROPERTIES(Game PROPERTIES
	COMPILE_DEFINITIONS _UNICODE
	RUNTIME_OUTPUT_DIRECTORY ${BIN_DIR}
	)
	
